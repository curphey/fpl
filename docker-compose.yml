version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data  # SQLite persistence
    env_file: .env
    environment:
      - DATABASE_PATH=/app/data/fpl.db
    restart: unless-stopped
