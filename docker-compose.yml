services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data  # SQLite persistence
    env_file:
      - path: .env
        required: false
    environment:
      - DATABASE_PATH=/app/data/fpl.db
    restart: unless-stopped
